{
  "name": "saspdashboard",
  "version": "0.1.0",
  "private": true,
  "description": "SASP dashboard — Italian-bank product simulator with live Finanzaonline ingestion",
  "scripts": {
    "dev": "vercel dev",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "@vercel/kv": "^2.0.0",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.5.0"
  }
}
